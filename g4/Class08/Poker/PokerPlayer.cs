using System;
using Cards;

namespace Poker
{
    public class PokerPlayer : Player
    {
        internal HandValue GetHandValue()
        {
            // 1. check if all cards are different
            // hint use grouping
            return null;
        }
    }
}